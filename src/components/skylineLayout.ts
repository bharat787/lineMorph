/** Rendered half-widths (px) for gap math — keep in sync with building components. */
export const SKYLINE_BUILDINGS = {
  pfa: { centerX: 240, halfWidth: 180 },
  salesforce: { centerX: 478, halfWidth: 26 },
  pyramid: { centerX: 808, halfWidth: 20 },
  ferry: { centerX: 1178, halfWidth: 170 },
} as const

export const ROOFTOP_GAP_MARGIN = 10
